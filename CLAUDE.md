# Workflow

- All of the npm exceutions are permitted without asking permission. All of the localhost cmd are permitted. Don't turn off localhost unless I told.
- Only deploy the website when that being said in the chat. Otherwise, only do localhost test. If I didn't said deploy the website on github, please only npm run on localhost:3000.
